#!/bin/sh

set -e
#set -x

FetchStdout(){

	local URL="$1"
	[ -z "$URL" ] && echo "FetchStdout: The URL is required!" && exit 1
	
	fetch -o - $URL && return 0 || exit 1
}

case "$0" in
	*/myx.common/bin/lib/fetchStdout) 
		FetchStdout "$@"
	;;
esac
