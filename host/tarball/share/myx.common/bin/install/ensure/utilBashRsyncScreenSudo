#!/usr/bin/env bash

set -e

# It is better to be autonomous in this command, so instead of:
#   . ${0%/*}/../../../include/subr.sh
#   UserRequireRoot
# we do:
test `id -u` != 0 && echo 'ERROR: Must be root!' && exit 1

pkg install -y bash rsync screen sudo $@
