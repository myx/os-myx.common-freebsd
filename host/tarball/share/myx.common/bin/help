#!/usr/bin/env bash

set -e

. ${0%/*}/../include/subr.sh



echo "'myx.common' script commands:"

for ITEM in $(find "${MYX_COMMON_BIN}" -type f | sed "s,${MYX_COMMON_BIN}/,,g" | sort); do
	echo "  $ITEM"
done

